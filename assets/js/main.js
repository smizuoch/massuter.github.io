// 各モジュールの初期化など
document.addEventListener('DOMContentLoaded', () => {
    console.log('Site Initialized');
    // 他のJSモジュールの初期化関数を呼び出す
    // initThemeToggle(); // theme-toggle.js 内で実行されるかも
    // initSmoothTransition(); // smooth-transition.js 内で実行
    // if (document.getElementById('gallery-auth')) {
    //   initAuth(); // auth.js 内で実行
    // }
    // if (document.querySelector('.gallery-grid')) {
    //   initGallery(); // gallery.js 内で実行
    // }
  });